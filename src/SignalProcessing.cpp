// Project includes
#include "include/SignalProcessing.h"
#include "include/BaseInstrument.h"
// STL includes
#include <map>
#include <tuple>

#define PEAK_THRESHOLD 5
#define SIGNAL_GAIN 1.0

std::map<double, double> generateAmplitudeFrequencyMap(double *frequencies[], std::tuple<double> frequency_range){
    std::map<double, double> amplitude_frequency_map; 

    return amplitude_frequency_map; 

}
double harmonicProductSpectrum(std::map<double, double> amplitude_frequency_map){ 
    double hps; 

    return hps;
} 

bool inTune(double hps){
    bool tuning;

    return tuning; 
}

double tuningCorrectionFactor(double hps, BaseInstrument instrument){
    double tuning_correction; 

    return tuning_correction; 
}

double calculateStandardDeviation(double *frequency_list_pointer) {
    double std_dev; 
    return std_dev; 
}

